@page
@model Razor9_identity.Pages_Blog.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>@Model.Article.Title</h1>
<p> <i>@Model.Article.Created</i></p>
<hr>
<div>
    
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Article.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Article.Content)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Article?.Id" class="btn btn-warning">Sửa bài viết</a> |
    <a asp-page="./Index" class="btn btn-primary">Về Trang danh sách</a>
</div>
